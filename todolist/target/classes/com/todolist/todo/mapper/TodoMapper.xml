<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespcae="package + interface" -->
<mapper namespace="com.todolist.todo.mapper.TodoMapper">
	<!-- 1. 게시판 리스트 -->
	<!-- id="method" -->
	<select id="list" resultType="com.todolist.todo.vo.TodoVO">
		select no, title, content, startDate, endDate
		from todo
		
		
	</select>
</mapper>